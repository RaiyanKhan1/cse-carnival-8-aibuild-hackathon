# Copy to .env to connect the prompt bar on the dashboard to your agent.
# Leave unset and the prompt bar runs in "Not connected" demo mode.
# Must be prefixed with VITE_ to be exposed to the browser by Vite.
VITE_AGENT_ENDPOINT=http://localhost:3000/api/agent
